/*
 * [문제 23-2] [정렬의 기준 지정하기]
 *
 * 아래의 main 메소드를 실행하면 TreeSet<Integer> 인스턴스에는 오름차순으로 정렬된 상태
 * 가 유지되면서 정수가 저장된다.
 *
 *     public static void main(String[] args) {
 *         TreeSet<Integer> tr = new TreeSet<>();
 *         tr.add(30);
 *         tr.add(10);
 *         tr.add(20);
 *         System.out.println(tr);
 *     }
 *
 * 따라서 main 메소드의 실행 결과는 다음과 같다.
 *     C:\JavaStudy>java ComparatorInteger
 *     [10, 20, 30]
 *
 * 이에 크고 작음에 대한 기준을 수정한 예제를 작성해보자. 즉 위의 main 메소드를 기반으로 다
 * 음의 실행 결과를 보이도록 코드를 수정 및 추가해보자.
 *     C:\JavaStudy>java ComparatorInteger
 *     [30, 20, 10]
 */

public class Answer01 {
    public static void main(String[] args) {

    }
}
